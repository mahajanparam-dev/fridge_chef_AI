# 👨‍🍳 Smart Chef AI Assistant

A dual-mode Python application that helps users find recipes using the Spoonacular API.

## 🚀 Features
- **Web Interface**: A polished UI built with Streamlit.
- **Smart Logic**: Handles greetings, off-topic questions, and recipe searches.
- **Live API**: Fetches real-time data from thousands of global recipes.

## 🛠️ Setup Instructions
1. Clone the repo: `git clone https://github.com/YOUR_USERNAME/Smart-Chef-App.git`
2. Install requirements: `pip install -r requirements.txt`
3. Add your `SPOONACULAR_API_KEY` to a `.env` file.
4. Run the web app: `streamlit run main.py`